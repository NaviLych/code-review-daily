# AI代码审查模式库

> 记录AI生成代码的常见问题模式，供审查时快速识别

---

## 一、性能问题模式

### 1.1 重复计算陷阱 ⭐⭐⭐⭐⭐

**识别特征**：
- 循环内部调用高开销函数（toLowerCase, JSON.parse等）
- filter后立即sort，形成双重遍历
- 每次渲染重新计算而非缓存

**典型代码**：
```javascript
// ❌ AI常犯：每次渲染都计算
const filtered = data.filter(item => expensiveOperation(item));
const sorted = [...filtered].sort(comparator);

// ✅ 正确：使用useMemo缓存
const processed = useMemo(() => {
    return data.filter(item => expensiveOperation(item))
               .sort(comparator);
}, [data]);
```

**审查要点**：
- [ ] 是否有useMemo/useCallback包裹？
- [ ] 依赖数组是否正确？
- [ ] 是否有防抖优化输入响应？

---

### 1.2 不必要的数组拷贝

**识别特征**：
- `[...array]`后紧跟sort
- spread操作符用于非必要场景
- 大量小对象spread创建GC压力

**典型代码**：
```javascript
// ❌ AI常犯：sort是就地算法，拷贝多余
const sorted = [...unsorted].sort((a,b) => a-b);

// ✅ 正确：直接排序
unsorted.sort((a,b) => a-b);
```

---

### 1.3 内存泄漏模式

**识别特征**：
- useEffect无return清理函数
- 事件监听器未移除
- WebSocket/定时器未关闭

**典型代码**：
```javascript
// ❌ AI常犯：缺少清理
useEffect(() => {
    const ws = new WebSocket(url);
    ws.onmessage = handler;
}, [url]);

// ✅ 正确：清理函数
useEffect(() => {
    const ws = new WebSocket(url);
    ws.onmessage = handler;
    return () => ws.close();
}, [url]);
```

---

## 二、逻辑错误模式

### 2.1 假设未验证 ⭐⭐⭐⭐⭐

**识别特征**：
- AI声称"基于X假设，Y成立"
- 使用外部库常量但未查证
- 依赖"看起来合理"的行为

**典型案例**（Claude Code生产Bug）：
```python
# ❌ AI假设drop_params会删除参数名和值
response = llm.chat(
    fallbacks=[{"model": "...", "reasoning_effort": "disable"}],
    drop_params=True  # 实际上只删除不支持的参数名，不删除不支持的参数值
)
```

**审查要点**：
- [ ] 是否验证了外部库的实际行为？
- [ ] 是否检查了关键常量的实际值？
- [ ] 是否在文档中明确标注了假设？

---

### 2.2 条件判断错误 ⭐⭐⭐⭐

**识别特征**：
- 索引从1开始 vs 从0开始的混淆
- 边界条件判断反向（< vs <=）
- 逻辑运算符错误（&& vs ||）

**典型代码**：
```javascript
// ❌ AI常犯：边界条件错误
if (index > array.length) {  // 应该是 >=
    return array[index - 1];
}

// ✅ 正确
if (index >= array.length) {
    return array[array.length - 1];
}
```

---

### 2.3 数学知识错误 ⭐⭐⭐

**识别特征**：
- 公式推导错误
- 运算顺序错误
- 统计量计算错误（方差、协方差等）

**典型代码**：
```python
# ❌ AI常犯：样本方差用n而非n-1
variance = sum((x - mean)**2 for x in data) / len(data)

# ✅ 正确：样本方差
variance = sum((x - mean)**2 for x in data) / (len(data) - 1)
```

---

### 2.4 Wrong Variable Return ⭐⭐

**识别特征**：
- 计算正确但返回了错误的变量
- 多个相似变量名导致混淆
- 条件分支返回不一致

**典型代码**：
```python
# ❌ AI常犯：结果存错变量
max_product = max(a*b for a,b in zip(list1, list2))
min_product = min(a*b for a,b in zip(list1, list2))
return max_product  # 题目要求返回最小，乘法却返回了最大

# ✅ 正确
return min_product
```

---

## 三、数据结构问题模式

### 3.1 不适当的数据结构选择

**识别特征**：
- 需要保持顺序时使用了set
- 需要去重时使用了list导致O(n²)
- 缺少对应数据结构的特性考虑

**典型代码**：
```javascript
// ❌ AI常犯：set丢失顺序
const duplicates = [...new Set(items)];  // 顺序丢失！

// ✅ 正确：保持顺序去重
const seen = new Set();
const unique = items.filter(item => {
    if (seen.has(item.id)) return false;
    seen.add(item.id);
    return true;
});
```

---

### 3.2 API返回处理错误

**识别特征**：
- 假设API返回可变类型
- 未处理Promise/Stream的异步特性
- 缺少错误处理

**典型代码**：
```javascript
// ❌ AI常犯：未处理API返回特性
const result = api.streamData();  // 返回不可变流
result.push(additionalData);  // 失败！

// ✅ 正确：使用正确方法
const result = api.streamData().toList();
result.push(additionalData);
```

---

## 四、安全问题模式

### 4.1 SQL注入漏洞 ⭐⭐⭐⭐⭐

**识别特征**：
- 字符串拼接构建SQL
- 用户输入直接进入查询
- 缺少输入验证

**典型代码**：
```python
# ❌ AI常犯：SQL注入风险
query = f"SELECT * FROM users WHERE name = '{username}'"
cursor.execute(query)

# ✅ 正确：参数化查询
query = "SELECT * FROM users WHERE name = %s"
cursor.execute(query, (username,))
```

---

### 4.2 硬编码秘密

**识别特征**：
- API密钥、密码写在代码中
- 数据库连接字符串明文
- 环境变量未外部化

**典型代码**：
```python
# ❌ AI常犯：硬编码密钥
API_KEY = "sk-xxxx-secret-key-here"

# ✅ 正确：环境变量
API_KEY = os.environ.get("API_KEY")
```

---

### 4.3 权限校验缺失

**识别特征**：
- 前端隐藏入口误以为后端受保护
- 信任客户端输入
- 缺少服务端权限验证

**典型代码**：
```javascript
// ❌ AI常犯：前端隐藏不保护后端
{/* <button onClick={handleAdmin}>Admin</button> */}
// 后端API仍然开放！
app.post('/api/admin', async (req, res) => {
    // 没有检查权限
    await adminOperation(req.body);
});
```

---

## 五、异步和并发问题模式

### 5.1 缺少async/await

**识别特征**：
- fetch调用后未await
- Promise链不完整
- 错误处理缺失

**典型代码**：
```javascript
// ❌ AI常犯：遗漏await
function getData() {
    const result = fetch('/api/data');  // 返回Promise而非数据
    return result;  // 返回类型错误
}

// ✅ 正确
async function getData() {
    const response = await fetch('/api/data');
    return response.json();
}
```

---

### 5.2 竞态条件

**识别特征**：
- useEffect中直接使用state闭包
- 依赖外部状态但不加入依赖数组
- 缺少取消/中止机制

**典型代码**：
```javascript
// ❌ AI常犯：闭包持有旧state
useEffect(() => {
    fetchData().then(data => setData(data));  // data是旧值
}, []);

// ✅ 正确：使用ref或正确的依赖
useEffect(() => {
    let cancelled = false;
    fetchData().then(data => {
        if (!cancelled) setData(data);
    });
    return () => { cancelled = true; };
}, []);
```

---

## 六、代码质量反模式

### 6.1 过度工程化

**识别特征**：
- 简单问题引入复杂抽象
- 创建大量适配器/包装类
- 违反KISS原则

**典型案例**（Claude Code 8小时会话）：
- 问题：MQTT URL格式错误（tcp://tcp://...）
- AI行为：创建MQTTServiceAdapter、consumerWrapper等多个适配器
- 结果：删除3421行代码，问题仍未解决

---

### 6.2 命名不当

**识别特征**：
- 使用data、temp等模糊名称
- 匈牙利命名法混用
- 中英文混用

**典型代码**：
```javascript
// ❌ AI常犯：模糊命名
const d = data.filter(t => t.v);

// ✅ 正确：清晰命名
const activeUsers = users.filter(user => user.isActive);
```

---

## 七、快速审查清单

### AI代码必查项

```
□ 性能
  □ 有无重复计算？
  □ 是否需要useMemo/useCallback？
  □ 有无内存泄漏（事件监听、WebSocket、定时器）？

□ 边界条件
  □ 空输入处理了吗？
  □ 极端值测试了吗？
  □ 索引越界可能吗？

□ 逻辑正确性
  □ 假设验证了吗？
  □ 外部依赖行为确认了吗？
  □ 条件判断方向正确吗？

□ 安全
  □ SQL参数化了吗？
  □ 输入验证了吗？
  □ 秘密外部化了吗？
  □ 权限检查了吗？

□ 异步
  □ await处理了吗？
  □ 错误捕获了吗？
  □ cleanup函数有吗？

□ 架构
  □ 过度设计了吗？
  □ 违反KISS/YAGNI了吗？
  □ 职责清晰吗？
  □ 事务内是否有外部API调用？
  □ 金融计算是否有溢出保护？
  □ 外部API响应是否用强类型DTO解析？

□ AI重构风险
  □ filter/条件判断背后是否有隐性业务规则？
  □ 方法拆分是否破坏了数据依赖？
  □ 每个业务规则是否有注释保护？

□ 异步桥接
  □ continuation 在所有路径上都 resume 了吗？
  □ Task 取消处理了吗？
  □ 超时保护有了吗？
  □ delegate 是否 weak？

□ 结构化并发
  □ 使用了 WithContext 传播取消吗？
  □ 并发有限制吗？
  □ error 有上下文包装吗？
  □ 共享变量写入安全吗？

□ 所有权循环
  □ 双向引用用 Weak 了吗？
  □ Debug trait 安全吗？
  □ RefCell borrow 会不会冲突？
```

---

## 八、案例索引

| 案例 | 来源 | 问题类型 | 严重度 |
|------|------|----------|--------|
| LiteLLM fallback | GitHub #54070 | 假设未验证 | ⭐⭐⭐⭐⭐ |
| Gemini缓存策略 | GitHub #54070 | 常量未验证 | ⭐⭐⭐⭐⭐ |
| argparse flags | GitHub #53610 | 假设未验证 | ⭐⭐⭐⭐ |
| React UserList | CSDN博客 | 性能问题 | ⭐⭐⭐ |
| WebSocket泄漏 | CSDN博客 | 内存泄漏 | ⭐⭐⭐⭐ |
| 3421行删除 | GitHub #6787 | 过度工程化 | ⭐⭐⭐⭐⭐ |
| SQL注入 | 多来源 | 安全漏洞 | ⭐⭐⭐⭐⭐ |
| TossPayment支付 | GitHub PR #84 | 事务内外部调用 | ⭐⭐⭐⭐⭐ |
| TossPayment支付 | GitHub PR #84 | Map类型安全缺失 | ⭐⭐⭐⭐ |
| Swift Continuation泄漏 | CodeStudy博客 | 异步桥接路径遗漏 | ⭐⭐⭐⭐⭐ |
| Go errgroup模式 | go-patterns.dev | 结构化并发好设计 | ⭐⭐⭐⭐⭐ |
| Rust Rc循环引用 | Rust Book Ch15-6 | 所有权循环泄漏 | ⭐⭐⭐⭐ |
| Swift async let crash | GitHub #81771 | 编译器回归Bug | ⭐⭐⭐⭐⭐ |
| TossPayment支付 | GitHub PR #84 | 整数运算溢出 | ⭐⭐⭐ |
| 账单重复计算 | 真实生产事故 | AI重构隐性规则丢失 | ⭐⭐⭐⭐⭐ |

---

*最后更新：2026-05-07*  
*来源：GitHub Issues/PRs、学术论文、技术博客、真实生产事故*

### 5.3 事务内外部调用陷阱 ⭐⭐⭐⭐⭐

**识别特征**：
- @Transactional方法内调用外部API（HTTP/RPC/MQ）
- 数据库锁持有时间覆盖了外部调用时间
- 外部调用失败导致事务回滚但锁已持有

**典型案例**（TossPayment支付确认）：
```java
// ❌ 事务内外部调用：锁持有覆盖了网络调用时间
@Transactional
public PaymentResponse confirmPayment(User user, PaymentConfirmRequest request) {
    Payment payment = repository.findByOrderIdForUpdate(request.getOrderId()); // 加悲观锁
    // ... 校验 ...
    Map<String, Object> response = tossPaymentClient.confirmPayment(request); // 外部API调用！
    payment.confirm(request.getPaymentKey(), method);
    return PaymentResponse.from(payment);
}

// ✅ 改进：先调外部API，再开事务更新（最短锁持有时间）
public PaymentResponse confirmPayment(User user, PaymentConfirmRequest request) {
    Payment payment = repository.findByOrderId(request.getOrderId()).orElseThrow(...);
    validate(user, payment, request);                        // 无事务校验
    TossConfirmResponse response = tossClient.confirm(request); // 外部调用（无锁）
    return updatePaymentStatus(request, response);           // 单独事务+锁
}
```

**审查要点**：
- [ ] @Transactional方法内是否有外部API/RPC调用？
- [ ] 数据库锁持有时间是否覆盖了网络调用时间？
- [ ] 外部调用失败时的回滚策略是否合理？

---

### 5.4 AI重构隐性规则丢失 ⭐⭐⭐⭐⭐

**识别特征**：
- AI将大方法拆分时，某些看似"计算逻辑"的代码实为"业务规则"
- filter/map操作被"优化"到其他层
- 业务约束只存在于代码中，无注释、无文档

**典型案例**（账单重复计算事故）：
```java
// 原代码
transactions = transactions.stream()
    .filter(t -> t.getBillId() == null)  // 看似简单null过滤，实际是"流水不能重复出账"
    .collect(Collectors.toList());

// AI重构后 —— filter被"优化"掉
List<Transaction> transactions = queryBillData(merchantId, startDate, endDate);
BigDecimal totalAmount = calculateBillAmount(transactions); // 未过滤，已出账流水重复计算
```

**防御策略**：
```java
// ✅ 每个filter/条件判断加上业务规则注释
/**
 * [业务规则] 结算协议4.2条：同一笔流水只能归入一期账单。
 * billId不为null表示该流水已被归入历史账单，当期不得重复计算。
 */
transactions = transactions.stream()
    .filter(t -> t.getBillId() == null)
    .collect(Collectors.toList());
```

**审查要点**：
- [ ] 每个filter/条件判断是否有业务规则注释？
- [ ] AI重构时是否逐条对照规则清单？
- [ ] 隐性规则是否已文档化？
